/**
 * Web stub. react-native-google-mobile-ads is native-only, so there are no
 * native ads on web. Metro resolves this in place of NativeAdCard.tsx for web.
 */
export function NativeAdCard(_props: { className?: string }) {
  return null;
}
